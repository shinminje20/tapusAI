# TapusAI Backend Configuration
# Copy this to .env and customize

# Application
APP_NAME=TapusAI Waitlist API
APP_VERSION=0.1.0
DEBUG=false

# Database (SQLite for development, PostgreSQL for production)
DATABASE_URL=sqlite+aiosqlite:///./tapusai.db
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/tapusai

# Waitlist Configuration
DEFAULT_AVG_TURN_TIME_MINUTES=15
